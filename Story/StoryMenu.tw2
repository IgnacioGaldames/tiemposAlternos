::StoryMenu[haml]
[[$nameStart|Start]]
<<if $showMarket is true>>[[$nameMarket->Market]]<</if>>
<<if $showInventory is true>>[[$nameInventory->Inventory]]<</if>>
<<if $showLog is true>>[[$nameLog->Log]]<</if>>
<<if $showPlayer is true>>[[$namePlayer->Player]]<</if>>